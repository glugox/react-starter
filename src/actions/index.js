import { login, loginSuccess, loginFailure, logout } from './auth'
import { showError, showAlert, hideAlert } from './alert'
import { toggleMenu } from './layout'

/**
 * Here we have core app actions
 */
export { 
    login, loginSuccess, loginFailure, logout,
    showError, showAlert, hideAlert,
    toggleMenu
} 